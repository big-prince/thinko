<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/styles.css">
    <script src="https://kit.fontawesome.com/6dcb090a5e.js" crossorigin="anonymous"></script>
    <title>Thinko | Login</title>
</head>
<style>
     @import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Dosis:wght@200;300;400;500;600;700;800&family=Fjalla+One&family=Fjord+One&family=Montserrat:wght@100;200&family=Mooli&family=Philosopher:ital,wght@0,400;0,700;1,400;1,700&family=Playfair+Display:wght@700&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Young+Serif&display=swap');

        body {
            margin: 0;
            padding: 0;
            font-family: 'Young Serif', sans-serif;
            background: linear-gradient(160deg, #42abfb, #05518b);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        h1 {
            text-align: center;
            font-size: 3rem;
            color: white;
            margin-right: 1rem;
        }

        h4 {
            text-align: center;
            color: white;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            padding: 0 2rem;
        }

        .form-container {
            background: rgba(255, 255, 255, 0.8);
            border-radius: 20px;
            padding: 20px;
            margin: 10px;
            width: 100%;
            box-shadow: 0px 0px 15px 0px #000;
            transition: transform 0.2s;
            transform: translateX(0);
        }

        h2 {
            margin: 0;
            color: #333;
        }

        a {
            color: #d5ad0a;
        }

        input {
            width: 100%;
            padding: 10px;
            margin: 8px 0;
            border: none;
            border-radius: 5px;
        }

        .glow-button {
            width: 100%;
            padding: 10px;
            margin-top: 10px;
            border: none;
            border-radius: 15px;
            background: linear-gradient(160deg, #42abfb, #05518b);
            color: #fff;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.3s, box-shadow 0.3s;
        }

        .glow-button:hover {
            background: linear-gradient(160deg, #42abfb, #05518b);
            box-shadow: 0px 0px 15px 0px #000;
        }

        .active .form-container {
            transform: translateX(0);
        }

        .backdrop {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(5px);
            z-index: 9998;
            display: none;
        }

        .popup {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background: white;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 1.5rem;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            z-index: 9999;
            display: none;
        }

        .popup h2 {
            margin-top: 0;
        }

        .popup input[type="password"] {
            margin: 10px 0;
            padding: 8px;
        }

        .popup button {
            padding: 8px 20px;
            background: #052742;
            color: white;
            border: none;
            cursor: pointer;
        }
    /* responsive styling for mobile according to styling above */
    @media (max-width: 768px) {
        body {
            flex-direction: column;
            padding: 2rem;
            margin: 0;
            justify-content: center;
            align-items: center;
            padding-top: 0;
            background: linear-gradient(120deg, #42abfb, #05518b);
        };

        h1 {
            margin-right: 0;
            font-size: 20px;
            margin-top: 0;
            margin: 0;
            padding-top: 0;
        }
        
        h4 {
            margin: 0;
            padding-top: 5rem;
            
        }

        .container {
            padding: 0;
            margin: 0;
            padding-top: 0;
            padding-bottom: 0;
        }
    }
    
    
</style>
<body>
    <header>
        <h1 style="color:  white;
        ">Welcome to Thinko <span style="size:inherit; color: #f7cd22;"><i class="fa-solid fa-lines-leaning"></i></span></h1>
        <!--Add nice catchy writeup for a quiz project-->
        <h4 style="margin-top: -5rem;align-items: center;font-family: 'Mooli', sans-serif;">Create Quizzes, Answer Quizzes.</h4>
    </header>
    <div class="container">
        <div class="form-container login-container" id="login-card">
            <h2>Login <i class="fa-sharp fa-solid fa-arrow-right-to-bracket"></i></h2>
            <form action="#" method="POST">
                <input type="text" placeholder="Username" id="login-username" required>
                <input type="password" placeholder="Password" id="login-password" required>
                <button class="glow-button" id="loginBtn">Login</button>
                <p>Don't have an account? <a href="" id="signup-link">Sign Up</a></p>
            </form>
        </div>
        <div class="form-container signup-container"  id="signup-card" style="display: none;">
            <h2>Create an Account <i class="fa-solid fa-user-plus" ;"></i></h2>
            <form action="#" method="POST">
                <input type="text" placeholder="Name" name="name" id="signup-name" required>
                <input type="text" placeholder="Username" name="username" id="signup-username" required>
                <input type="password" placeholder="Password"  name="password" id="signup-password" required>
                <button class="glow-button" id="signupBtn">Sign Up</button>
                <p>Already have an account? <a href="" id="login-link">Login</a></p>
            </form>
        </div>
    </div>
</body>
<script>
    const loginCard = document.getElementById('login-card');
    const signupCard = document.getElementById('signup-card');
    const signupLink = document.getElementById('signup-link');
    const loginLink = document.getElementById('login-link');
    const signupBtn = document.getElementById('signupBtn');
    const loginBtn = document.getElementById('loginBtn');

    //code to show login or signup
    function showLoginForm() {
    loginCard.style.display = 'block';
    signupCard.style.display = 'none';
     }

    function showSignupForm() {
    loginCard.style.display = 'none';
    signupCard.style.display = 'block';
    }


    // Event listeners to show login or signup forms
    signupLink.addEventListener('click', function(event) {
    event.preventDefault();
    showSignupForm();
    });

    loginLink.addEventListener('click', function(event) {
    event.preventDefault();
    showLoginForm();
     });

    
    //signup Button submit
    signupBtn.addEventListener('click', async(e)=>{
        e.preventDefault()

        //fetch api
        const sName = document.getElementById('signup-name');
        const sUsername = document.getElementById('signup-username');
        const sPassword = document.getElementById('signup-password');

        //url and api
        const url = '/signup'
        const response = await fetch(url, {
            method: 'POST',
            headers: {
                "Content-Type": "application/json"
            },
            credentials: "include",
            body: JSON.stringify({
                name: sName.value,
                username: sUsername.value,
                password: sPassword.value
            })
        })

        const data = await response.json()
        if(response.ok){
            window.location.href = '/home'
        }
    })

    //login handler
    loginBtn.addEventListener('click', async(e)=>{
        e.preventDefault()

        //require inputs
        const loginUsername = document.getElementById('login-username');
        const loginPassword = document.getElementById('login-password')

        //fetch api
        const url = '/login'
        const response = await fetch(url, {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            credentials: "include",
            body: JSON.stringify({
                username: loginUsername.value,
                password: loginPassword.value
            })
        });

        const data = await response.json()
        if(response.ok){
            window.location.href = '/home'
        }
    })
</script>
</html>
